name: Execution Control Workflow

on:
    workflow_dispatch

jobs:
    ParentJobExecution:
        runs-on: ubuntu-latest
        steps:
            - name: Parent Job Execution
              run: |
                sleep 15
                echo "This step executed after 30s"
    ChildJobExecution:
        runs-on: ubuntu-latest
        needs: ParentJobExecution
        steps:
            - name: Child Job Execution
              run: echo "This job is executed after Parent job"