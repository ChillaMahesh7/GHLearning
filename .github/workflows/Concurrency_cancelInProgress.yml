name: Concurrency Practice_CancelInProgress

on:
    workflow_dispatch
concurrency:
    group: concurrency-${{ github.sha }}
    cancel-in-progress: true

jobs:
    CocurrencyPractice:
        runs-on: ubuntu-latest
        steps:
            - name: Testing Concurrency
              run: |
                sleep 30
                echo "Concurrency testing"